// API configuration - use relative /api paths since frontend is served by same backend
// No fetch interceptor needed - browser will make requests to same origin
console.log('API configured: relative /api paths (same origin)');
