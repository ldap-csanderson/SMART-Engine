# Application Configuration
app:
  name: "SMART Engine"
  version: "3.0.0"
  host: "0.0.0.0"
  port: 8000

# Google Cloud Platform Settings
gcp:
  project_id: "csanderson-experimental-443821"
  region: "us-central1"
  credentials_path: "${GCP_SERVICE_ACCOUNT_KEY_PATH}"

# BigQuery Settings
bigquery:
  dataset: "smart_engine_data"
  connection: "us-central1.vertex-ai-connection"
  tables:
    dataset_items: "dataset_items"
    dataset_embeddings: "dataset_embeddings"
    gap_analysis_results: "gap_analysis_results"
    filter_results: "filter_results"
  models:
    gemini_flash: "gemini-flash"
    text_embeddings: "text-embeddings"
  default_page_size: 100
  filter_batch_size: 500

# Google Ads Settings
google_ads:
  customer_id: ""
  config_path: "../scripts/google-ads.yaml"

# OAuth Settings (for in-app Google Ads re-authorization)
oauth:
  # Must be added as an Authorized Redirect URI in Google Cloud Console
  # for OAuth client: 759167631809-1ibgmfql9fp8o6be06nub9q5pcle8117.apps.googleusercontent.com
  redirect_uri: "https://smart-engine-xdzhjknata-uc.a.run.app/api/auth/google-ads/callback"

# Secret Manager Settings
secrets:
  google_ads_secret_name: "google-ads-yaml"

# API Settings
api:
  max_retries: 3
  retry_delay_seconds: 5
